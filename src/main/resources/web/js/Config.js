"use strict";

var config = {
    DEBUG_MAP_DRAW: false,             // to debug map's drawing on the canvas
    DEBUG_DRAW_CITY: false,            // to debug the cities drawing on the canvas
    DEBUG_AJAX: false,                 // to debug AJAX requests to the Server
    DEBUG_SERVER_NOTIFICATIONS: true,  // to debug PUSH-notifications from the Server

    TILE_WIDTH: 80,
    TILE_HEIGHT: 80,
    isShowTileBorders: false
};
